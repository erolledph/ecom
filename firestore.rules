rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Users collection: Users can only read/write their own profile
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Stores collection: Public read, owner write/create/delete
    match /stores/{storeId} {
      allow read; // Anyone can read store details
      allow create: if request.auth != null && request.auth.uid == storeId && request.auth.uid == request.resource.data.ownerId;
      allow update, delete: if request.auth != null && request.auth.uid == resource.data.ownerId;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.ownerId;
    }

    // Products subcollection: Public read, owner write
    match /stores/{storeId}/products/{productId} {
      allow read; // Anyone can read products from any store
      allow create, update, delete: if request.auth != null && request.auth.uid == storeId;
    }

    // Slides subcollection: Public read, owner write
    match /stores/{storeId}/slides/{slideId} {
      allow read; // Anyone can read slides from any store
      allow create, update, delete: if request.auth != null && request.auth.uid == storeId;
    }

    // Categories subcollection: Public read, owner write
    match /stores/{storeId}/categories/{categoryId} {
      allow read; // Anyone can read categories from any store
      allow create, update, delete: if request.auth != null && request.auth.uid == storeId;
    }
  }
}